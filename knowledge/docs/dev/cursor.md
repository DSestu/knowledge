# Cursor

## Function args as first suggestions in Cursor

Enable "locality bonus" to get function args as first suggestions in Cursor *(findable in UI settings too)*:

`"editor.suggest.localityBonus": true`
